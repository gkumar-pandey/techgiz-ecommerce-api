import { Request, Response } from 'express';
import User from '../models/user';

/**
 * @route PUT /api/v1/users/:userId
 * @description
 * @param {*} req
 * @param {*} res
 */
const updateUserDetailsHandler = async (req: Request, res: Response) => {
  try {
  } catch (error) {}
};
